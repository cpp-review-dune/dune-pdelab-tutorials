add_latex_document(exercise_grid1.tex
                   IMAGE_DIRS .
                   FORCE_PDF
                  )

add_latex_document(exercise_grid2.tex
                   IMAGE_DIRS .
                   FORCE_PDF
                  )
