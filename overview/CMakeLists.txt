add_latex_document(abstractions.tex
                   BIBFILES abstractions.bib
                   DEFAULT_PDF
                   MANGLE_TARGET_NAMES
                  )

add_latex_document(overview.tex
                   DEFAULT_PDF
                   MANGLE_TARGET_NAMES
                  )
